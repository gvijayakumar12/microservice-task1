server:
  port: 8084

spring:
  application:
    name: auth-module
  datasource:
    url: jdbc:mysql://localhost:3306/ms_1_auth?useSSL=false
    username: root
    password: password
  
  jpa:
    hibernate:
      ddl-auto: update
      
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8085/eureka